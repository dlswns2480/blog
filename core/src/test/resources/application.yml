spring:
  datasource:
    url: jdbc:mysql://localhost:3306/junlog
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        highlight_sql: true # ANSI 코드에 색을 나타낸다
        default_batch_fetch_size: 10

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace